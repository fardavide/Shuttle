apply plugin: 'shuttle.android.compose.library'

optIns {
    experimentalMaterial3Api()
    experimentalPermissionsApi()
}

// modules
dependencies {
    implementation projects.shuttle.accessibility
    implementation projects.shuttle.apps.domain
    implementation projects.shuttle.design
    implementation projects.shuttle.icons.domain
    implementation projects.shuttle.payments.domain
    implementation projects.shuttle.payments.presentation
    implementation projects.shuttle.permissions.domain
    implementation projects.shuttle.settings.domain
    implementation projects.shuttle.utils.android

    androidTestImplementation projects.shuttle.test.compose
}

dependencies {
    implementation libs.accompanist.permissions
    implementation libs.androidx.lifecycle.viewModel
    implementation libs.bundles.base
    implementation libs.bundles.compose
    implementation libs.koin.android
    implementation libs.navigation.compose

    debugImplementation libs.compose.uiTooling

    testImplementation libs.bundles.test.kotlin
    androidTestImplementation libs.bundles.test.android
    androidTestImplementation libs.compose.uiTest
}
