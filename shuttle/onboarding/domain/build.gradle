apply plugin: 'shuttle.kotlin'

moduleDependencies {
    implementation 'settings:domain'
    implementation 'utils:kotlin'
}

dependencies {
    implementation libs.bundles.base
    testImplementation libs.bundles.test.kotlin
}
